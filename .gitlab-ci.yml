stages:
    - test
    - package
    - deploy

variables:
    DOCKER_REGISTRY: dockerhub.ebi.ac.uk
    K8S_REPO_URL: https://github.com/EBIvariation/eva-k8s.git
    MAVEN_SETTINGS: maven-settings.xml
    URL_MAVEN_SETTINGS: https://api.github.com/repos/EBIvariation/configuration/contents/eva-maven-settings.xml
    KUBECONFIG_FILE_DEV: https://api.github.com/repos/EBIvariation/configuration/contents/kubernetes-clusters/development-k8s-team-admin-conf.conf
    KUBECONFIG_FILE_STAGING: https://api.github.com/repos/EBIvariation/configuration/contents/kubernetes-clusters/staging-k8s-team-admin-conf.conf
    MEDIA_TYPE: application/vnd.github.raw

# REQUIRED GitLab CI/CD Variables (Global or per-project):
# DOCKER_REGISTRY_USER: Username for Docker registry authentication
# DOCKER_REGISTRY_PASSWORD: Password for Docker registry authentication
# GITHUB_USER: GitHub username for accessing private maven settings repository
# GITHUB_TOKEN: GitHub personal access token for accessing private maven settings repository
#
# ============================================================================
# Test
# ============================================================================

test:
    stage: test
    image: maven:3.9-eclipse-temurin-21-alpine
    services:
        - docker:dind
    variables:
        DOCKER_HOST: tcp://docker:2375
        DOCKER_TLS_CERTDIR: ""
        DOCKER_DRIVER: overlay2
    script:
        - mvn -B clean test
    environment:
        name: test-env
    only:
        - master
        - tags

# ============================================================================
# Base Job Templates
# ============================================================================

.build_docker:
    stage: package
    image: docker:latest
    services:
        - name: docker:dind
          alias: docker
          command: ["--storage-driver=overlay2", "--insecure-registry=localhost"]
    variables:
        DOCKER_DRIVER: overlay2
        DOCKER_TLS_CERTDIR: ""
        DOCKER_HOST: tcp://docker:2375
    before_script:
        - echo "Waiting for Docker daemon to be ready..."
        - docker info
        - echo -n $DOCKER_REGISTRY_PASSWORD | docker login -u $DOCKER_REGISTRY_USER --password-stdin $DOCKER_REGISTRY
    script:
        - docker build -f $DOCKERFILE_PATH -t $DOCKER_IMAGE:$IMAGE_TAG -t $DOCKER_IMAGE:$IMAGE_LATEST .
        - docker push $DOCKER_IMAGE:$IMAGE_TAG
        - docker push $DOCKER_IMAGE:$IMAGE_LATEST

.update_and_deploy:
    stage: deploy
    image: alpine:latest
    before_script:
        - apk add --no-cache git curl python3 kubectl
        - python3 -m venv $HOME/.venv
        - source $HOME/.venv/bin/activate
        - git config --global user.email "gitlab-ci@ebi.ac.uk"
        - git config --global user.name "GitLab CI"
        - git config --global credential.helper store
        # Clone eva-k8s for scripts and manifests
        - git clone --depth 1 https://github.com/EBIvariation/eva-k8s.git k8s-repo
        - cd k8s-repo
        - pip install --no-cache-dir lxml retry cached-property pyyaml requests
        - pip install --no-dependencies -r scripts/requirements.txt
        - echo "https://${GITHUB_TOKEN}:x-oauth-basic@github.com" > ~/.git-credentials
        - mkdir -p ~/.kube
        # Download kubernetes cluster configuration from private repository
        - curl -u $GITHUB_USER:$GITHUB_TOKEN -H "Accept:$MEDIA_TYPE" $KUBECONFIG_FILE > ~/.kube/config
        - chmod 600 ~/.kube/config
    script:
        - source $HOME/.venv/bin/activate
        # Download Maven settings from private repository
        - curl -u $GITHUB_USER:$GITHUB_TOKEN -H "Accept:$MEDIA_TYPE" $URL_MAVEN_SETTINGS > maven-settings.xml
        # Convert Maven settings to application.properties
        - python scripts/maven-settings-to-properties.py --maven_file maven-settings.xml --property_set $PROPERTY_SET --profile $MAVEN_PROFILE --output application.properties
        # Update image tag in kustomization
        - python scripts/update-image-tag.py "$K8S_MANIFEST_PATH/kustomization.yaml" "${IMAGE_TAG}"
        - git add "$K8S_MANIFEST_PATH/kustomization.yaml"
        # Only commit if the image tag has changed
        - |
            if ! git diff --cached --quiet; then
              git commit -m "Update $SERVICE_NAME ${ENV} image tag to ${IMAGE_TAG}"
              git push https://${GITHUB_TOKEN}:x-oauth-basic@github.com/EBIvariation/eva-k8s.git main
            fi

        # Copy the application.properties where it is expected -- DO NOT COMMIT
        - cp -vf application.properties $K8S_MANIFEST_PATH/

        # Deploy to cluster
        - kubectl apply -k $K8S_MANIFEST_PATH
        - kubectl rollout status deployment/$SERVICE_NAME -n $K8S_NAMESPACE --timeout=5m
    environment:
        kubernetes:
            namespace: $K8S_NAMESPACE

# ============================================================================
# eva-server
# ============================================================================

build:docker:eva-server:dev:
    extends: .build_docker
    variables:
        DOCKERFILE_PATH: eva-server/Dockerfile
        DOCKER_IMAGE: $DOCKER_REGISTRY/ebivariation/eva-server
        IMAGE_TAG: dev-$CI_COMMIT_SHORT_SHA
        IMAGE_LATEST: dev-latest
    only:
        - master

deploy:eva-server:dev:
    extends: .update_and_deploy
    needs:
        - build:docker:eva-server:dev
    variables:
        SERVICE_NAME: eva-server
        PROPERTY_SET: eva-server
        IMAGE_TAG: dev-$CI_COMMIT_SHORT_SHA
        ENV: development
        MAVEN_PROFILE: development
        K8S_MANIFEST_PATH: k8s-manifests/eva-server/overlays/dev
        K8S_NAMESPACE: eva-server-dev
        KUBECONFIG_FILE: $KUBECONFIG_FILE_DEV
    environment:
        name: development
    only:
        - master

deploy:eva-server:staging:
    extends: .update_and_deploy
    needs:
        - build:docker:eva-server:dev
    variables:
        SERVICE_NAME: eva-server
        PROPERTY_SET: eva-server
        IMAGE_TAG: dev-$CI_COMMIT_SHORT_SHA
        ENV: staging
        MAVEN_PROFILE: production_processing
        K8S_MANIFEST_PATH: k8s-manifests/eva-server/overlays/staging
        K8S_NAMESPACE: eva-server-stage
        KUBECONFIG_FILE: $KUBECONFIG_FILE_STAGING
    environment:
        name: staging
    only:
        - master

# ============================================================================
# eva-release
# ============================================================================

build:docker:eva-release:dev:
    extends: .build_docker
    variables:
        DOCKERFILE_PATH: eva-release/Dockerfile
        DOCKER_IMAGE: $DOCKER_REGISTRY/ebivariation/eva-release
        IMAGE_TAG: dev-$CI_COMMIT_SHORT_SHA
        IMAGE_LATEST: dev-latest
    only:
        - master

deploy:eva-release:dev:
    extends: .update_and_deploy
    needs:
        - build:docker:eva-release:dev
    variables:
        SERVICE_NAME: eva-release
        PROPERTY_SET: eva-release
        IMAGE_TAG: dev-$CI_COMMIT_SHORT_SHA
        ENV: development
        MAVEN_PROFILE: development
        K8S_MANIFEST_PATH: k8s-manifests/eva-release/overlays/dev
        K8S_NAMESPACE: eva-release-dev
        KUBECONFIG_FILE: $KUBECONFIG_FILE_DEV
    environment:
        name: development
    only:
        - master

deploy:eva-release:staging:
    extends: .update_and_deploy
    needs:
        - build:docker:eva-release:dev
    variables:
        SERVICE_NAME: eva-release
        PROPERTY_SET: eva-release
        IMAGE_TAG: dev-$CI_COMMIT_SHORT_SHA
        ENV: staging
        MAVEN_PROFILE: production_processing
        K8S_MANIFEST_PATH: k8s-manifests/eva-release/overlays/staging
        K8S_NAMESPACE: eva-release-stage
        KUBECONFIG_FILE: $KUBECONFIG_FILE_STAGING
    environment:
        name: staging
    only:
        - master

# ============================================================================
# count-stats
# ============================================================================

build:docker:count-stats:dev:
    extends: .build_docker
    variables:
        DOCKERFILE_PATH: count-stats/Dockerfile
        DOCKER_IMAGE: $DOCKER_REGISTRY/ebivariation/count-stats
        IMAGE_TAG: dev-$CI_COMMIT_SHORT_SHA
        IMAGE_LATEST: dev-latest
    only:
        - master

deploy:count-stats:dev:
    extends: .update_and_deploy
    needs:
        - build:docker:count-stats:dev
    variables:
        SERVICE_NAME: count-stats
        PROPERTY_SET: count-stats
        IMAGE_TAG: dev-$CI_COMMIT_SHORT_SHA
        ENV: development
        MAVEN_PROFILE: development
        K8S_MANIFEST_PATH: k8s-manifests/count-stats/overlays/dev
        K8S_NAMESPACE: count-stats-dev
        KUBECONFIG_FILE: $KUBECONFIG_FILE_DEV
    environment:
        name: development
    only:
        - master

deploy:count-stats:staging:
    extends: .update_and_deploy
    needs:
        - build:docker:count-stats:dev
    variables:
        SERVICE_NAME: count-stats
        PROPERTY_SET: count-stats
        IMAGE_TAG: dev-$CI_COMMIT_SHORT_SHA
        ENV: staging
        MAVEN_PROFILE: production_processing
        K8S_MANIFEST_PATH: k8s-manifests/count-stats/overlays/staging
        K8S_NAMESPACE: count-stats-stage
        KUBECONFIG_FILE: $KUBECONFIG_FILE_STAGING
    environment:
        name: staging
    only:
        - master


# ============================================================================
# dgva-server
# ============================================================================

build:docker:dgva-server:dev:
    extends: .build_docker
    variables:
        DOCKERFILE_PATH: dgva-server/Dockerfile
        DOCKER_IMAGE: $DOCKER_REGISTRY/ebivariation/dgva-server
        IMAGE_TAG: dev-$CI_COMMIT_SHORT_SHA
        IMAGE_LATEST: dev-latest
    only:
        - master

deploy:dgva-server:dev:
    extends: .update_and_deploy
    needs:
        - build:docker:dgva-server:dev
    variables:
        SERVICE_NAME: dgva-server
        PROPERTY_SET: dgva-server
        IMAGE_TAG: dev-$CI_COMMIT_SHORT_SHA
        ENV: development
        MAVEN_PROFILE: development
        K8S_MANIFEST_PATH: k8s-manifests/dgva-server/overlays/dev
        K8S_NAMESPACE: dgva-server-dev
        KUBECONFIG_FILE: $KUBECONFIG_FILE_DEV
    environment:
        name: development
    only:
        - master

deploy:dgva-server:staging:
    extends: .update_and_deploy
    needs:
        - build:docker:dgva-server:dev
    variables:
        SERVICE_NAME: dgva-server
        PROPERTY_SET: dgva-server
        IMAGE_TAG: dev-$CI_COMMIT_SHORT_SHA
        ENV: staging
        MAVEN_PROFILE: production_processing
        K8S_MANIFEST_PATH: k8s-manifests/dgva-server/overlays/staging
        K8S_NAMESPACE: dgva-server-stage
        KUBECONFIG_FILE: $KUBECONFIG_FILE_STAGING
    environment:
        name: staging
    only:
        - master
